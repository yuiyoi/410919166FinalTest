
@{
    ViewBag.Title = "Index";
}

<h2>時間查詢</h2>

@using (Html.BeginForm("Index", "TimeSearch", FormMethod.Get))
{
    <p>姓名:@Html.TextBox("name")</p>
    <p>數字:@Html.TextBox("number")</p>

    <p>Hi @ViewBag.yourName, 查詢時間為 : @ViewBag.result</p>

    <input type="submit" value="送出" />
}